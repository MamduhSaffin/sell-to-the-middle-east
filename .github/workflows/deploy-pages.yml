name: Deploy GCC Market Entry to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_BASE_PATH: ""
      NEXT_PUBLIC_GA_ID: "G-SYBHPLSVCS"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: TypeScript check
        run: pnpm check

      - name: Add company profile and seller success videos to the seller guide
        run: node scripts/inject-video-sections.mjs

      - name: Apply approved eRomman Google Drive visuals
        run: node scripts/apply-approved-drive-visuals.mjs

      - name: Download approved eRomman website images
        run: |
          mkdir -p public/images/approved
          curl -fL --retry 3 --retry-delay 2 \
            "https://drive.google.com/uc?export=download&id=19fFneYa9XuEMYwIf_lE5ewyPc9ZSMmpi" \
            -o public/images/approved/01-eRomman-GCC-Roll-Up-Banner.png
          curl -fL --retry 3 --retry-delay 2 \
            "https://drive.google.com/uc?export=download&id=1oMVCO_eK9yA0ITfxPsYhMttnz9cm34QI" \
            -o public/images/approved/03-eRomman-GCC-Landing-Page-Hero.png
          curl -fL --retry 3 --retry-delay 2 \
            "https://drive.google.com/uc?export=download&id=18VeQbB_dpM7vJS_4WxhclxIfunxO4p6M" \
            -o public/images/approved/04-eRomman-GCC-Consumer-Discovery-Infographic.png
          curl -fL --retry 3 --retry-delay 2 \
            "https://drive.google.com/uc?export=download&id=1zeA__K9ym1J9X6QdDhvXbj0kHv6xOas9" \
            -o public/images/approved/05-eRomman-GCC-Website-Brochure.png
          curl -fL --retry 3 --retry-delay 2 \
            "https://drive.google.com/uc?export=download&id=1Ae2FhKHrRMS2z37iNa2xLB15GiiihfXs" \
            -o public/images/approved/06-eRomman-GCC-Official-Market-Entry-Hero.png
          test -s public/images/approved/01-eRomman-GCC-Roll-Up-Banner.png
          test -s public/images/approved/03-eRomman-GCC-Landing-Page-Hero.png
          test -s public/images/approved/04-eRomman-GCC-Consumer-Discovery-Infographic.png
          test -s public/images/approved/05-eRomman-GCC-Website-Brochure.png
          test -s public/images/approved/06-eRomman-GCC-Official-Market-Entry-Hero.png

      - name: Build static export
        run: pnpm build

      - name: Validate routes and fingerprint this release
        run: node scripts/verify-pages.mjs stamp ./out "$GITHUB_SHA" "$NEXT_PUBLIC_BASE_PATH"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: https://gccmarketentry.me/
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout verification script
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Confirm the release at the custom domain
        timeout-minutes: 15
        run: node scripts/verify-pages.mjs live "https://gccmarketentry.me/" "$GITHUB_SHA"
